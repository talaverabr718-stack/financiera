@extends('clients.layout')
@section('title','Productos financieros')
@section('content')
<x-page-header title="Productos financieros" description="Define las reglas para nuevas solicitudes y créditos." eyebrow="Configuración editable"><x-slot:actions><a href="{{route('products.create')}}" class="btn-primary">Nuevo producto</a></x-slot:actions></x-page-header>
<div class="mb-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-xs leading-5 text-amber-800">Los cambios aplican únicamente a operaciones nuevas. Las condiciones aprobadas se conservan dentro de cada crédito.</div>
<section class="grid gap-4 lg:grid-cols-2">@forelse($products as $product)<article class="card p-5"><div class="flex items-start justify-between"><div><p class="text-[10px] font-semibold uppercase tracking-wide text-indigo-500">{{$product->code}}</p><h2 class="mt-1 text-base font-semibold">{{$product->name}}</h2></div><span class="rounded-full px-2.5 py-1 text-[10px] font-semibold {{$product->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}}">{{$product->is_active?'Activo':'Inactivo'}}</span></div><div class="mt-5 grid grid-cols-2 gap-4 text-xs"><div><p class="text-slate-400">Tasa predeterminada</p><p class="mt-1 font-semibold">{{$product->default_interest_rate?$product->default_interest_rate.'%':'Sin definir'}}</p></div><div><p class="text-slate-400">Método</p><p class="mt-1 font-semibold">{{$product->default_interest_method?:'Sin definir'}}</p></div><div><p class="text-slate-400">Plazo permitido</p><p class="mt-1 font-semibold">{{$product->minimum_term}} - {{$product->maximum_term}} cuotas</p></div><div><p class="text-slate-400">Mora</p><p class="mt-1 font-semibold">{{$product->delinquency_method?:'Sin definir'}}</p></div></div><div class="mt-5 flex justify-end"><a href="{{route('products.edit',$product)}}" class="flex items-center gap-2 rounded-lg bg-[#eeebff] px-3 py-2 text-xs font-semibold text-indigo-600"><i data-lucide="pencil" class="icon"></i>Editar reglas</a></div></article>@empty<div class="card p-12 text-center text-sm text-slate-400">No existen productos configurados.</div>@endforelse</section>
@endsection
