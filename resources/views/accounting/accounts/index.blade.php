@extends('clients.layout') @section('title','Catálogo de cuentas') @section('content') @include('accounting.tabs')
<x-page-header title="Catálogo de cuentas" description="Estructura jerárquica utilizada por los asientos." eyebrow="Contabilidad"><x-slot:actions><a href="{{route('accounting.accounts.create')}}" class="btn-primary"><i data-lucide="plus" class="icon"></i>Nueva cuenta</a></x-slot:actions></x-page-header>
<section class="card overflow-hidden"><div class="table-wrap"><table class="w-full text-left"><thead class="text-[10px] uppercase text-slate-400"><tr><th class="table-cell">Código</th><th class="table-cell">Cuenta</th><th class="table-cell">Clasificación</th><th class="table-cell">Naturaleza</th><th class="table-cell">Uso</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($accounts as $account)<tr><td class="table-cell font-semibold text-indigo-600">{{$account->code}}</td><td class="table-cell"><span style="padding-left:{{max(0,$account->level-1)*16}}px" class="font-medium">{{$account->name}}</span><p class="text-[10px] text-slate-400">{{$account->parent?->name}}</p></td><td class="table-cell">{{$account->type_label}}</td><td class="table-cell">{{$account->nature==='debit'?'Deudora':'Acreedora'}}</td><td class="table-cell"><span class="badge {{$account->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}}">{{$account->is_postable?'Imputable':'Agrupadora'}} · {{$account->is_active?'Activa':'Inactiva'}}</span></td><td class="table-cell text-right"><a href="{{route('accounting.accounts.edit',$account)}}" class="text-xs font-semibold text-indigo-600">Editar</a></td></tr>@empty<tr><td colspan="6"><x-empty-state title="Catálogo vacío" description="Crea las cuentas aprobadas para la financiera; no se cargaron cuentas ficticias." /></td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 p-3">{{$accounts->links()}}</div></section>@endsection
