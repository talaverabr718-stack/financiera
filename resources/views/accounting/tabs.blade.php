@php $tabs=[['accounting.dashboard','Resumen'],['accounting.accounts.index','Catálogo'],['accounting.entries.index','Asientos'],['accounting.journal','Diario'],['accounting.ledger','Mayor'],['accounting.trial','Balance de comprobación']]; @endphp
<nav class="mb-4 flex gap-1 overflow-x-auto rounded-xl border border-slate-200 bg-white p-1" aria-label="Navegación contable">@foreach($tabs as [$route,$label])<a href="{{route($route)}}" class="shrink-0 rounded-lg px-3 py-2 text-xs font-semibold {{request()->routeIs(str_replace(['.index'],['.*'],$route))||request()->routeIs($route)?'bg-indigo-600 text-white':'text-slate-500 hover:bg-slate-50'}}">{{$label}}</a>@endforeach</nav>
