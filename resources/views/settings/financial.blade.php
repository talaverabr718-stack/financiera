@extends('clients.layout')
@section('title','Configuración financiera')
@section('content')
<div class="mb-6"><h1 class="text-[28px] font-semibold">Políticas financieras</h1><p class="mt-1 text-sm text-slate-500">Las condiciones se administran por producto y solo aplican a operaciones nuevas.</p></div>@include('settings._tabs')
<div class="mb-5 flex justify-end"><a href="{{route('products.create')}}" class="rounded-xl bg-indigo-500 px-4 py-2.5 text-xs font-semibold text-white">+ Nuevo producto</a></div>
<section class="card overflow-hidden"><div class="border-b border-slate-100 p-5"><h2 class="text-sm font-semibold">Productos crediticios</h2><p class="mt-1 text-xs text-slate-500">Tasas, métodos, mora, frecuencia, plazos y orden de aplicación.</p></div><div class="divide-y divide-slate-100">@forelse($products as $product)<div class="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-semibold">{{$product->name}} <span class="ml-2 text-[10px] text-slate-400">{{$product->code}}</span></p><p class="mt-1 text-xs text-slate-500">{{$product->currency}} · Tasa {{filled($product->default_interest_rate)?$product->default_interest_rate.'%':'sin definir'}} · {{$product->is_active?'Activo':'Inactivo'}}</p></div><a href="{{route('products.edit',$product)}}" class="text-xs font-semibold text-indigo-500">Editar reglas →</a></div>@empty<div class="p-8 text-center text-sm text-slate-500">Aún no hay productos configurados.</div>@endforelse</div></section>
<section class="card mt-5 p-5"><h2 class="text-sm font-semibold">Parámetros críticos globales</h2><p class="mt-1 text-xs text-slate-500">Se muestran sin inventar valores. Su edición se habilitará cuando cada definición tenga una regla operacional aprobada.</p><div class="mt-4 grid gap-3 md:grid-cols-2">@forelse($criticalSettings as $setting)<div class="rounded-xl border border-slate-100 p-4"><p class="text-xs font-semibold">{{$setting->label}}</p><p class="mt-1 text-[11px] text-slate-500">{{$setting->description}}</p><span class="mt-3 inline-block rounded-full px-2 py-1 text-[10px] font-semibold {{is_null($setting->value)?'bg-amber-50 text-amber-700':'bg-emerald-50 text-emerald-600'}}">{{is_null($setting->value)?'Sin configurar':'Configurado'}}</span></div>@empty<p class="text-xs text-slate-500">No hay definiciones globales registradas.</p>@endforelse</div></section>
@endsection
