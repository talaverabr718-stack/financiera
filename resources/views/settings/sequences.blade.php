@extends('clients.layout')
@section('title','Consecutivos')
@section('content')
<div class="mb-6"><h1 class="text-[28px] font-semibold">Consecutivos documentales</h1><p class="mt-1 text-sm text-slate-500">Ajusta la presentación de números futuros sin reutilizar ni retroceder folios.</p></div>@include('settings._tabs')
@if($errors->any())<div class="mb-5 rounded-xl bg-rose-50 p-4 text-sm text-rose-700"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{$error}}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{route('settings.sequences.update')}}">@csrf @method('PUT')<section class="card overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-xs"><thead class="bg-slate-50 text-slate-500"><tr><th class="px-5 py-3">Documento</th><th class="px-5 py-3">Prefijo</th><th class="px-5 py-3">Dígitos</th><th class="px-5 py-3">Siguiente número protegido</th><th class="px-5 py-3">Vista previa</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($sequences as $sequence)<tr><td class="px-5 py-4 font-semibold">{{str($sequence->key)->replace('_',' ')->title()}}</td><td class="px-5 py-4"><input name="sequences[{{$sequence->key}}][prefix]" value="{{$sequence->prefix}}" class="w-28 rounded-lg border border-slate-200 px-3 py-2" required></td><td class="px-5 py-4"><input type="number" min="3" max="12" name="sequences[{{$sequence->key}}][padding]" value="{{$sequence->padding}}" class="w-20 rounded-lg border border-slate-200 px-3 py-2" required></td><td class="px-5 py-4 font-mono text-slate-500">{{$sequence->next_number}}</td><td class="px-5 py-4 font-mono text-indigo-600">{{$sequence->prefix}}{{str_pad((string)$sequence->next_number,$sequence->padding,'0',STR_PAD_LEFT)}}</td></tr>@endforeach</tbody></table></div></section><div class="mt-5 flex justify-end"><button class="rounded-xl bg-indigo-500 px-5 py-2.5 text-xs font-semibold text-white">Guardar formatos</button></div></form>
@endsection
