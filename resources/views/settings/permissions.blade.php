@extends('clients.layout')
@section('title','Permisos')
@section('content')
<div class="mb-6"><h1 class="text-[28px] font-semibold">Permisos por usuario</h1><p class="mt-1 text-sm text-slate-500">Asigna acceso de consulta y administración por cada módulo.</p></div>@include('settings._tabs')
<form method="POST" action="{{route('settings.permissions.update')}}">@csrf @method('PUT')<section class="card overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-xs"><thead class="bg-slate-50"><tr><th class="sticky left-0 bg-slate-50 px-4 py-3">Usuario</th>@foreach($modules as $module)<th class="min-w-32 px-3 py-3 text-center">{{$module->name}}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($users as $user)<tr><td class="sticky left-0 bg-white px-4 py-4"><strong>{{$user->name}}</strong><span class="block text-[10px] text-slate-400">{{$user->email}}</span></td>@foreach($modules as $module)@php $grant=$grants->get($user->id.'-'.$module->id); @endphp<td class="px-3 py-4"><div class="flex justify-center gap-3"><label title="Ver" class="flex items-center gap-1 text-[10px]"><input type="checkbox" name="permissions[{{$user->id}}][{{$module->id}}][view]" value="1" @checked(!$grant||$grant->can_view)>Ver</label><label title="Administrar" class="flex items-center gap-1 text-[10px]"><input type="checkbox" name="permissions[{{$user->id}}][{{$module->id}}][manage]" value="1" @checked($grant?->can_manage)>Editar</label></div></td>@endforeach</tr>@empty<tr><td colspan="{{count($modules)+1}}" class="p-8 text-center text-slate-500">No hay usuarios registrados.</td></tr>@endforelse</tbody></table></div></section><div class="mt-5 flex justify-end"><button class="rounded-xl bg-indigo-500 px-5 py-2.5 text-xs font-semibold text-white">Guardar permisos</button></div></form>
@endsection
