@extends('clients.layout')
@section('title','Integración contable')
@section('content')
<div class="mb-6"><h1 class="text-[28px] font-semibold">Integración contable</h1><p class="mt-1 text-sm text-slate-500">Relaciona la operación financiera con cuentas activas y afectables del catálogo.</p></div>@include('settings._tabs')
@if($errors->any())<div class="mb-5 rounded-xl bg-rose-50 p-4 text-sm text-rose-700"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{$error}}</li>@endforeach</ul></div>@endif
@php $labels=['cash_account_id'=>'Caja general','bank_account_id'=>'Bancos','loan_receivable_account_id'=>'Cartera de préstamos','interest_income_account_id'=>'Ingresos por intereses','fee_income_account_id'=>'Ingresos por cargos'];$input='mt-1.5 w-full rounded-xl border border-slate-200 px-3.5 py-2.5 text-sm outline-none focus:border-indigo-400'; @endphp
<form method="POST" action="{{route('settings.accounting.update')}}">@csrf @method('PUT')<section class="card p-6"><div class="grid gap-5 md:grid-cols-2">@foreach($labels as $key=>$label)<label class="text-xs font-medium text-slate-600">{{$label}}<select name="{{$key}}" class="{{$input}}"><option value="">Sin asignar</option>@foreach($accounts as $account)<option value="{{$account->id}}" @selected((string)old($key,$settings[$key]??'')===(string)$account->id)>{{$account->code}} · {{$account->name}}</option>@endforeach</select></label>@endforeach</div><div class="mt-6 rounded-xl bg-amber-50 p-4 text-xs text-amber-800">Estas asignaciones no generan asientos automáticos por sí solas. Cada integración deberá usar transacciones, trazabilidad e idempotencia.</div></section><div class="mt-5 flex justify-end"><button class="rounded-xl bg-indigo-500 px-5 py-2.5 text-xs font-semibold text-white">Guardar integración</button></div></form>
@endsection
