@extends('clients.layout')
@section('title','Configuración de módulos')
@section('content')
<div class="mb-6"><h1 class="text-[28px] font-semibold">Módulos del sistema</h1><p class="mt-1 text-sm text-slate-500">Desactiva el acceso, oculta opciones del menú y define su orden.</p></div>@include('settings._tabs')
<form method="POST" action="{{route('settings.modules.update')}}">@csrf @method('PUT')<div class="grid gap-4 md:grid-cols-2">@foreach($modules as $module)<section class="card p-5"><div class="flex items-start justify-between gap-4"><div><h2 class="text-sm font-semibold">{{$module->name}}</h2><p class="mt-1 text-xs text-slate-500">{{$module->description}}</p></div><input type="number" min="0" max="999" name="modules[{{$module->id}}][sort_order]" value="{{$module->sort_order}}" class="w-16 rounded-lg border border-slate-200 px-2 py-1.5 text-xs" aria-label="Orden"></div><div class="mt-5 flex flex-wrap gap-5"><label class="flex items-center gap-2 text-xs font-semibold text-slate-600"><input type="checkbox" name="modules[{{$module->id}}][enabled]" value="1" @checked($module->is_enabled) class="rounded text-indigo-500">Módulo activo</label><label class="flex items-center gap-2 text-xs font-semibold text-slate-600"><input type="checkbox" name="modules[{{$module->id}}][visible]" value="1" @checked($module->is_visible) class="rounded text-indigo-500">Visible en menú</label></div></section>@endforeach</div><div class="mt-5 flex justify-end"><button class="rounded-xl bg-indigo-500 px-5 py-2.5 text-xs font-semibold text-white">Guardar módulos</button></div></form>
@endsection
