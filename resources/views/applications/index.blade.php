@extends('clients.layout')
@section('title','Solicitudes de crédito')
@section('content')
@php $labels=['draft'=>'Borrador','submitted'=>'Enviada','review'=>'En revisión','approved'=>'Aprobada','rejected'=>'Rechazada','disbursed'=>'Desembolsada','cancelled'=>'Cancelada']; @endphp
<x-page-header title="Solicitudes" description="Evalúa y da seguimiento a las solicitudes de crédito." eyebrow="Crédito"><x-slot:actions><a href="{{route('products.index')}}" class="btn-secondary">Productos</a><a href="{{route('applications.create')}}" class="btn-primary"><i data-lucide="plus" class="icon"></i>Nueva solicitud</a></x-slot:actions></x-page-header>
<form class="card mb-4 flex flex-col gap-2 p-3 sm:flex-row"><input name="search" value="{{request('search')}}" placeholder="Buscar número o cliente" class="control mt-0 flex-1"><select name="status" class="control mt-0 sm:w-52"><option value="">Todos los estados</option>@foreach($labels as $value=>$label)<option value="{{$value}}" @selected(request('status')===$value)>{{$label}}</option>@endforeach</select><button class="btn-soft">Filtrar</button></form>
<section class="card overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left"><thead class="bg-slate-50 text-[10px] uppercase tracking-wide text-slate-400"><tr><th class="table-cell">Solicitud</th><th class="table-cell">Cliente</th><th class="table-cell">Producto</th><th class="table-cell">Monto</th><th class="table-cell">Responsable</th><th class="table-cell">Estado</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($applications as $application)<tr class="hover:bg-slate-50"><td class="table-cell text-xs font-semibold text-indigo-600">{{$application->number}}</td><td class="table-cell"><p class="text-xs font-semibold">{{$application->client->full_name}}</p><p class="text-[10px] text-slate-400">{{$application->client->code}}</p></td><td class="table-cell text-xs">{{$application->product->name}}</td><td class="table-cell text-xs font-semibold">C$ {{number_format((float)$application->requested_amount,2)}}</td><td class="table-cell text-xs">{{$application->seller->user->name}}</td><td class="table-cell"><span class="rounded-full bg-[#eeebff] px-2.5 py-1 text-[10px] font-semibold text-indigo-700">{{$labels[$application->status]}}</span></td><td class="table-cell"><a href="{{route('applications.show',$application)}}" class="text-xs font-semibold text-indigo-600">Ver →</a></td></tr>@empty<tr><td colspan="7" class="p-12 text-center text-sm text-slate-400">No hay solicitudes registradas.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 p-4">{{$applications->links()}}</div></section>
@endsection
